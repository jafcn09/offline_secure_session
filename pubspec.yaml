name: offline_secure_session
description: Zero-dependency Dart library for secure token storage with expiration, offline action queue, auto-sync on reconnection, and persistent cache. Works on all platforms.
version: 1.0.0
homepage: https://github.com/jafcn09/offline_secure_session
repository: https://github.com/jafcn09/offline_secure_session
issue_tracker: https://github.com/jafcn09/offline_secure_session/issues

topics:
  - offline
  - session
  - cache
  - queue
  - sync

environment:
  sdk: ">=2.12.0 <4.0.0"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
